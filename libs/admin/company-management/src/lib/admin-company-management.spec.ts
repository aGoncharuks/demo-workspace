import { adminCompanyManagement } from './admin-company-management';

describe('adminCompanyManagement', () => {
  it('should work', () => {
    expect(adminCompanyManagement()).toEqual('admin-company-management');
  });
});
